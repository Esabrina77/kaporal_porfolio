// Types pour React Three Fiber
export interface FloatingCubeProps {
  position: [number, number, number];
  color: string;
  speed: number;
}

export interface DistortedSphereProps {
  position: [number, number, number];
  color: string;
  speed: number;
}

export interface AudioPlayerProps {
  src: string;
  title: string;
}
